import { PriceDivisorsPipe } from './price-divisors.pipe';

describe('PriceDivisorsPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceDivisorsPipe();
    expect(pipe).toBeTruthy();
  });
});
